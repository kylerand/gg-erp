# Implementation Status

This file is the single source of truth for MVP implementation completion.

Use it to answer:

- what is done
- what is in progress
- what is blocked
- what still has to ship before MVP

Update this file in the same PR that changes implementation status. Supporting documents such as the dependency map, decision log, and sprint updates remain evidence sources, not competing trackers.

## Status rules

- `✅ Done`: implemented and evidenced by code, tests, handlers, migrations, or shipped contracts
- `⚠️ In Progress`: active implementation exists, but required contracts, tests, or read paths are still incomplete
- `⚠️ In Review`: implementation is complete enough for review, but validation or sign-off is still pending
- `❌ Blocked`: known blocker is preventing completion
- `❌ Not Started`: identified and scoped, but no implementation has landed yet

## Update rules

- Track feature slices or contract slices, not tiny code tasks.
- Every `✅ Done` row must include evidence.
- Use `Next action` to make the next move obvious.
- Keep detailed technical rationale in the architecture docs and ADRs.
- Keep runtime business data out of this file.

## MVP tracker

Last reviewed: 2026-06-01

| Feature / contract slice                                                                                                            | Domain               | Status         | Owner | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Blockers                                                                          | Next action                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------------- | -------------------- | -------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Auth session bootstrap contract (`GET /auth/me`)                                                                                    | Identity             | ✅ Done        | Team  | Runtime handler in `apps/api/src/lambda/identity/handlers.ts`; Lambda entry `apps/api/src/lambda/identity/me.handler.ts`; local route in `apps/api/src/server.ts`; API Gateway JWT route in `infra/terraform/modules/api-gateway-lambda/main.tf`; build entry in `scripts/build-lambdas.ts`; focused coverage in `apps/api/src/tests/auth-me.test.ts`; web client in `apps/web/src/features/auth/api.ts`; ERP shell floor-tech link now opens the floor auth handoff entrypoint and returns to the technician queue through Cognito SSO                                           | None for the `GET /auth/me` contract or current floor-tech handoff                | Keep payload aligned with role/permission UI and verify cross-origin SSO handoff in live UAT         |
| Customer create + lifecycle transition mutations                                                                                    | Identity             | ✅ Done        | Team  | Covered in `employee-web-api-dependency-map.md`; runtime handlers in `apps/api/src/lambda/customers/handlers.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | None                                                                              | Keep aligned with customer list/profile work                                                         |
| Customer list + profile update contracts                                                                                            | Identity             | ✅ Done        | Team  | `GET /identity/customers` and `PATCH /identity/customers/{id}` route wiring in `apps/api/src/server.ts`; customer handlers in `apps/api/src/lambda/customers/handlers.ts`; work-order customer drawer save path in `apps/web/src/app/work-orders/[id]/page.tsx`; coverage in `apps/api/src/tests/customer-cart-profile-editing.test.ts`                                                                                                                                                                                                                                           | None                                                                              | Expand standalone customer/dealer CRM history and asset relationship views                           |
| Dealer account and relationship read model                                                                                          | Identity             | ✅ Done        | Team  | Deployable Prisma migrations `20260601033000_add_customer_dealer_relationships` and `20260601034000_seed_customer_dealer_relationships` add dedicated dealer account and customer-dealer relationship storage; `GET /identity/dealers` reads `dealerAccount` rows with customer context; `GET /identity/dealer-relationships` returns dealer, customer, cart, and relationship context; web hub/dealer/relationship pages use strict live reads; coverage in `apps/api/src/tests/dealer-list.test.ts`, `apps/api/src/tests/dealer-relationships-list.test.ts`, `apps/web/src/lib/live-page-data.test.ts`, and `packages/test-utils/tests/schema-contracts.test.js` | None for read-side relationship model                                             | Add create/update dealer assignment workflows once relationship ownership mutation rules are finalized |
| Technician task + rework mutation flows                                                                                             | Tickets              | ✅ Done        | Team  | Technician task and rework route wiring in `apps/api/src/server.ts`; dispatch and work-order execution screens call live routes; rework coverage exists in ticket/QC tests; transition handler coverage now rejects invalid technician IDs, blocks starting unassigned tasks, and allows assign-and-start; My Queue refreshes from the server after rejected transitions                                                                                                                                                                                                          | None for current technician-task mutation contract                                | Keep extending conflict coverage when collaborator pools or shift handoff rules land                 |
| Time entry contract for work-order execution                                                                                        | Tickets              | ✅ Done        | Team  | `GET/POST/PATCH/DELETE /tickets/time-entries` route wiring in `apps/api/src/server.ts`; UI in `apps/web/src/app/work-orders/time-logging/page.tsx`; coverage in `apps/api/src/tests/time-entry.test.ts`                                                                                                                                                                                                                                                                                                                                                                           | None                                                                              | Add richer labor costing reports and payroll/accounting handoff later                                |
| SOP and OJT route/service contracts                                                                                                 | SOP/OJT              | ✅ Done        | Team  | Runtime route families in `apps/api/src/lambda/sop` and server wiring; training state coverage in `apps/api/src/tests/sop-training-state.test.ts`; repaired training UI, live API calls, manager assignment creation, and supervisor sign-off actions in the web app                                                                                                                                                                                                                                                                                                              | None for current runtime contracts                                                | Add content authoring/editing once those APIs are designed                                           |
| Reservation and shortage mutation path                                                                                              | Inventory            | ✅ Done        | Team  | `apps/api/src/tests/inventory-failure-cases.test.ts`; `apps/api/src/tests/inventory-scaffold-coverage.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | None                                                                              | Close the remaining read-model loop                                                                  |
| Inventory lot and reservation read contracts (`GET /inventory/lots`)                                                                | Inventory            | ✅ Done        | Team  | `GET /inventory/lots` route wiring in `apps/api/src/server.ts`; handler coverage in `apps/api/src/tests/inventory-lambda-handlers.test.ts` and `apps/api/src/tests/inventory-reads.test.ts`; reservation UI in `apps/web/src/app/inventory/reservations/page.tsx`                                                                                                                                                                                                                                                                                                                 | None                                                                              | Add item edit workflows and keep reservation reads aligned with movement history                     |
| Inventory movement ledger and adjustment posting contracts (`GET /inventory/ledger`, `POST /inventory/adjustments`)                 | Inventory            | ✅ Done        | Team  | `GET /inventory/ledger` and `POST /inventory/adjustments` route wiring in `apps/api/src/server.ts`; Lambda entries in `apps/api/src/lambda/inventory/list-ledger.handler.ts` and `apps/api/src/lambda/inventory/create-adjustment.handler.ts`; API Gateway routes in `infra/terraform/modules/api-gateway-lambda/main.tf`; build entries in `scripts/build-lambdas.ts`; handler coverage in `apps/api/src/tests/inventory-lambda-handlers.test.ts`; live ledger and stock-adjustment pages in `apps/web/src/app/inventory/ledger/page.tsx` and `apps/web/src/app/inventory/adjustments/page.tsx`; registry entries in `packages/domain/src/erp-object-registry.ts`; reservation create/release/issue and manual adjustment mutations append ledger consequences and stamp `last_ledger_entry_id` | None for movement history reads or manual adjustment posting                       | Extend transfers and cycle-count batches so every stock mutation appends full ledger consequences     |
| Receiving, PO progression, replenishment, default-vendor assignment, variance, payable handoff, import/export, and bulk row actions | Inventory/Accounting | ✅ Done        | Team  | `apps/api/src/tests/inventory-reads.test.ts`; `apps/api/src/tests/inventory-lambda-handlers.test.ts`; `apps/web/src/app/inventory/parts/page.tsx`; `apps/web/src/app/inventory/purchase-orders/page.tsx`; `apps/web/src/app/inventory/purchase-orders/[id]/page.tsx`; `apps/web/src/app/inventory/receiving/page.tsx`; `apps/web/src/app/inventory/planning/page.tsx`; `apps/web/src/app/accounting/page.tsx`; `apps/web/src/app/accounting/sync/page.tsx`                                                                                                                        | None                                                                              | Expand broader item editing and BOM selectors when mutation APIs are stable                          |
| Work-order and build-package selector reads                                                                                         | Planning             | ✅ Done        | Team  | `GET /planning/work-orders` and `GET /planning/build-packages` route wiring in `apps/api/src/server.ts`; build-package catalog handler in `apps/api/src/lambda/work-orders/handlers.ts`; handler coverage in `apps/api/src/tests/work-order-lambda-handlers.test.ts`; live catalog page in `apps/web/src/app/planning/build-packages/page.tsx`; work-order create selector wiring in `apps/web/src/app/work-orders/new/page.tsx`                                                                                                                                                  | None for selector reads                                                           | Add write-side build configuration and BOM authoring/versioning once dedicated tables land           |
| Build-slot planner board reads                                                                                                      | Planning             | ✅ Done        | Team  | `GET /scheduling/demand-projection`, `GET /scheduling/capacity-slots`, `POST /scheduling/capacity-slots/import`, `GET /scheduling/schedule-preview`, `POST /scheduling/schedule-publications`, and `GET /scheduling/schedule-assignments` route wiring; coverage in `apps/api/src/tests/build-planning-reads.test.ts`; planner UI in `apps/web/src/app/planning/slots/page.tsx`; projection payloads now include live-query freshness and explicit conflict objects for no-capacity, overflow, material, blocked-operation, and missing-estimate planner actions                                                                 | None for current planner read/publish contract                                    | Keep extending scheduling quality metrics and technician-skill matching as planning rules deepen     |
| Workspace summary and reporting snapshot reads                                                                                      | Reporting            | ✅ Done        | Team  | `GET /workspace/today` and `GET /reporting/snapshot` route wiring in `apps/api/src/server.ts`; Lambda packaging in `scripts/build-lambdas.ts`; API Gateway route wiring in `infra/terraform/modules/api-gateway-lambda/main.tf`; coverage in `apps/api/src/tests/workspace-today.test.ts` and `apps/api/src/tests/reporting-snapshot.test.ts`; shared snapshot/freshness contract in `packages/domain/src/erp-reports.ts`; reporting page consumes the single snapshot API with mock fallback disabled                                                                 | None for current summary/snapshot read model                                      | Add persisted report subscriptions and scheduled exports once subscription tables are designed       |
| Invoice sync mutation and detail flow                                                                                               | Accounting           | ✅ Done        | Team  | Covered in dependency map; failure coverage referenced in `invoice-sync-failure-cases.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | None                                                                              | Add list/filter monitor APIs                                                                         |
| Invoice, customer, and payment sync monitor reads                                                                                   | Accounting           | ✅ Done        | Team  | `GET /accounting/invoice-sync`, `GET /accounting/customers`, `GET /accounting/payment-syncs`, invoice retry, and payment retry route wiring in `apps/api/src/server.ts`; web client in `apps/web/src/lib/api-client.ts`; monitor UI in `apps/web/src/app/accounting/sync/page.tsx`; handler coverage in `apps/api/src/tests/sync-route-handlers.test.ts`                                                                                                                                                                                                                          | None                                                                              | Add escalation subscriptions and deeper financial report integration                                 |
| Audit log and observability mutation baseline                                                                                       | Platform             | ✅ Done        | Team  | Present across architecture docs and current mutation tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | None                                                                              | Keep new mutation routes aligned with audit and observability hooks                                  |
| ShopMonkey migration pipeline and historical backfill framework                                                                     | Migration            | ⚠️ In Progress | Team  | Root scripts include `migrate:shopmonkey` and `extract:shopmonkey-csvs`; sprint update says pipeline is nearly ready                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Final validation and rollout proving still open                                   | Run migration validation and document cutover readiness                                              |
| Reference data seed migration and production seed readiness                                                                         | Database             | ✅ Done        | Team  | Idempotent architecture reference seed in `apps/api/src/migrations/0005_seed_reference_data.sql`; deploy-applied Prisma-compatible migrations seed baseline ERP roles, permissions, grants, stock locations, stock bins, planning defaults, and dealer account/relationship bootstrap rows derived from live commercial customer records; stock-bin deploy-schema coverage exists in `packages/db/prisma/migrations/20260601031000_add_inventory_stock_bins/migration.sql` and `packages/db/prisma/migrations/20260601032000_seed_stock_bins/migration.sql`; dealer relationship coverage exists in `packages/db/prisma/migrations/20260601033000_add_customer_dealer_relationships/migration.sql` and `packages/db/prisma/migrations/20260601034000_seed_customer_dealer_relationships/migration.sql`; coverage in `packages/test-utils/tests/schema-contracts.test.js`; rollout guidance in `docs/architecture/postgresql-rollout-and-seeding.md` | None                                                                              | Run staging seed verification during cutover rehearsal and keep environment-specific data in runbooks |
| Fresh Prisma deploy reliability                                                                                                     | Database             | ✅ Done        | Team  | `packages/db/prisma/migrations/20260504020000_add_inventory_reservation_storage/migration.sql` drops duplicate check constraints before re-adding them, `scripts/check-prisma-migrations.mjs` now guards against re-adding inline constraints without a preceding drop, and a throwaway Postgres `prisma migrate deploy` applies all 29 migrations from an empty database                                                                                                                                                                                                        | None                                                                              | Keep fresh-deploy verification in the release checklist when migration tooling changes               |

## Working agreement

- If a detailed doc disagrees with this file, update this file first and then reconcile the supporting doc.
- If work is too large for one row, split it into contract-level slices.
- If ownership is unknown, keep `Team` temporarily instead of leaving the row out.
