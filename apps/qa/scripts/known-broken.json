{
  "_doc": "Routes with an expected non-healthy API smoke status in the currently deployed QA API. Smoke fails when this list drifts in EITHER direction — a new bug joins, OR an existing bug gets fixed/deployed (so we know to remove the entry). File a Linear ticket when adding long-lived handler bugs; remove deployment-staged entries after the route is live. Use expectRouteMiss for pending Terraform routes that are not deployed yet.",
  "broken": [
    {
      "method": "POST",
      "path": "/identity/dealers",
      "expectStatus": 404,
      "expectRouteMiss": true,
      "note": "Pending deploy for dealer account create route added in feat/dealer-account-create-update."
    },
    {
      "method": "PATCH",
      "path": "/identity/dealers/{id}",
      "expectStatus": 404,
      "expectRouteMiss": true,
      "note": "Pending deploy for dealer account update route added in feat/dealer-account-create-update."
    }
  ]
}
